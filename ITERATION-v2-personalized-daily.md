# AIPulse 迭代 v2：个性化日报 + Glance 仪表盘

> 写于 2026-06-12 · 目标：从"周更新闻墙"升级为"会学你口味的每日 AI 情报官"

## 一句话目标

**让 AIPulse 像一个懂你的助理**：每天早上给你一份 AI 行业日报，你点赞过的方向，明天它就多给你推；交互界面换成成熟的开源仪表盘 Glance，省去自己维护前端。

---

## 三大变化（这次要做的）

| 变化 | 现在 | 改成 | 为什么 |
|---|---|---|---|
| **① 学口味** | 所有人看到一样的内容 | 你点赞 → 明天多推同方向 | 信息爆炸，只看你关心的 |
| **② 频率** | 每周一更新一次 | 每天更新一次（日报） | 周更太慢，AI 圈一天一变 |
| **③ 界面** | 自己写的静态 HTML | 接入 Glance 开源仪表盘 | 不用自己维护前端，还能加天气/时钟等 |

---

## ⚠️ 先说一个关键发现（决定整个架构）

**Glance 是个"只读"的仪表盘** —— 它只能"展示"信息，本身**不能让用户点按钮把数据传回服务器**（比如点赞）。

打个比方：Glance 像一台**电视机**，能播节目，但你没法对着电视"点赞"让它记住。

**所以点赞功能不能指望 Glance 自己做**。解法是用 Glance 的一个特殊能力叫 **extension（扩展位）**：

- Glance 留了一个"画中画"的口子，可以把**我们自己做的一小块网页**嵌进去
- 我们在这块自己的网页里放"👍 点赞"按钮
- 按钮连回我们自己的服务器，记下你喜欢什么

```
┌─────────── Glance 仪表盘（电视机外壳）───────────┐
│  ┌────────┐  ┌────────┐  ┌──────────────────┐  │
│  │ 时钟    │  │ 天气    │  │ 【我们的画中画】   │  │
│  │ (原生)  │  │ (原生)  │  │  AI 日报 + 👍按钮  │  │
│  └────────┘  └────────┘  │  ↑ 点赞连回我们服务器│  │
│                          └──────────────────┘  │
└──────────────────────────────────────────────┘
```

这样分工：**Glance 管外壳和布局，我们管"会交互的 AI 情报流"**。

---

## 整体数据流（改造后）

```
每天早上 8:00（北京时间）GitHub Actions 自动跑：

  1. 抓数据（复用现有）
     RSS博客 + arXiv论文 + HN热议 + HF模型 → 一堆原始素材

  2. 个性化打分（新增）
     读「用户兴趣画像」→ DeepSeek 给每条打分
     你爱看的方向 = 高分；没兴趣的 = 低分

  3. 生成日报（升级现有 daily_digest）
     按分数排序 → 选当天最该看的 N 条 → 写入数据库

  4. 输出两份（新增 extension 端点）
     ① data/daily.json —— 给 Glance 读的数据
     ② /api/feedback —— 接收点赞的端点

用户侧：

  打开 Glance → 看到嵌入的 AI 日报 → 点👍
       ↓
  点赞写回 /api/feedback → 更新「用户兴趣画像」
       ↓
  明天第 2 步打分时，这个方向的权重就提高了
```

---

## 分阶段路线（P0 → P2）

### 🔴 P0：先验证 Glance 能不能嵌入我们的内容（1-2 天）

**为什么 P0**：整个方案的地基是"Glance 能嵌入我们带点赞按钮的网页"。这个没验证通，后面全白搭。先花半天 spike 验证，别一上来就写一堆代码。

**做什么**：
1. 本地 Docker 跑起来 Glance（`docker run glanceapp/glance`）
2. 写一个最小的 extension 端点：返回一段 HTML，里面一个"👍"按钮
3. 配 Glance 的 extension widget 指向这个端点
4. 确认：按钮能显示出来 + 点击能触发我们的服务器

**验收 gate**：
- ✅ Glance 里能看到我们的自定义 HTML 块
- ✅ 点按钮能在我们服务器日志里看到一条记录
- ❌ 如果 Glance extension 不支持 JS 交互 → 改用 Plan B（见下方"风险"）

**产出**：一个能跑的最小 demo，证明架构可行。

---

### 🟡 P1：日报 + 点赞学兴趣（1-2 周）

#### P1.1 频率改日更（半天）
- 把 `.github/workflows/weekly.yml` 的 cron 从 `0 0 * * 1`（每周一）改成 `0 0 * * *`（每天）
- `daily_digest.py` 已经有"今日3件事"逻辑，改成"今日 N 条"（N 可配，建议 8-12）
- ⚠️ 顺手把 `daily_digest.py` 里的 `UYILINK_API_KEY` 改成 `DEEPSEEK_API_KEY`（现在还是旧的）

#### P1.2 建「用户兴趣画像」（2-3 天）
新建一张表存用户喜好：

```sql
CREATE TABLE user_interest(
  user_id    TEXT,      -- 先支持单用户，填 'default'
  theme      TEXT,      -- 主题，如 "AI Agents" "RAG" "多模态"
  weight     REAL,      -- 兴趣权重，初始 1.0
  like_count INTEGER,   -- 累计点赞数
  updated_at TEXT
);
```

- 初始：所有主题权重 = 1.0（一视同仁）
- 每点赞一次：该主题 weight += 0.2（封顶 3.0，避免越滚越偏）
- 缓慢衰减：每天所有 weight × 0.98（你不再点的，慢慢退回中性）

#### P1.3 点赞端点（2 天）
新增 `api/feedback.py`：
- 收 POST：`{user_id, item_id, theme, action: "like"}`
- 更新 `user_interest` 表
- 加 rate limit（防刷）

#### P1.4 个性化打分（2-3 天）
改造 `daily_digest.py` 的排序逻辑：
- 原来：LLM 纯按"重要性"排
- 改成：`最终分 = LLM重要性分 × 该主题的用户兴趣权重`
- 你爱看 Agent → Agent 类文章排前面

#### P1.5 extension 端点（2 天）
新增 `api/glance_feed.py`：
- 返回 Glance extension 要的 HTML 格式
- 内容 = 今日日报 N 条 + 每条带"👍"按钮
- 按钮 onclick → 调 `/api/feedback`

**P1 验收 gate**：
- ✅ 每天早上 Glance 里能看到当天日报
- ✅ 点赞某条 → 数据库 user_interest 对应主题 weight 上涨
- ✅ 连续点赞"Agent"类 3 天 → 第 4 天 Agent 类明显排更前
- ✅ 诚实测试：如果点赞没影响排序，记 FAIL + 根因，不假装成功

---

### 🟢 P2：体验打磨（按需，1 个月内）

- **"为什么推这条"**：每条日报标注"因为你赞过 X"（让学习过程透明）
- **多维度兴趣**：不只主题，还学"偏爱论文 vs 偏爱产品新闻"
- **点踩**：不只点赞，也能点"不感兴趣"，负向调权重
- **兴趣面板**：一个小页面显示"你的兴趣画像"（哪些主题权重高）
- **RSS 源扩容**：配合 `rss_sources_extended.py`，从 14 源扩到 50 源

---

## 关键技术决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| **点赞 UI 放哪** | Glance extension 嵌入自己的 HTML | Glance 只读，原生做不了交互 |
| **兴趣存哪** | signals.db 新增 user_interest 表 | 复用现有数据库，零新依赖 |
| **打分用什么模型** | DeepSeek（已接入） | 已有 key，成本低 |
| **多用户？** | P1 先单用户（user_id='default'） | 种子阶段你自己用，别过度设计 |
| **权重算法** | 点赞 +0.2 / 每日衰减 ×0.98 | 简单可解释，不上复杂推荐算法 |

---

## 风险 & 应对

| 风险 | 触发信号 | 应对（Plan B） |
|---|---|---|
| **Glance extension 不支持 JS 交互** | P0 spike 点赞按钮点不动 | 点赞改成"点链接跳转"：👍 是个链接，点了打开 `/api/feedback?like=xxx`，跳转回来 |
| **Glance 嵌入样式难看** | 我们的 HTML 在 Glance 里排版乱 | 用 Glance 的 custom-css 调，或退回用我们自己的前端 + Glance 只做导航 |
| **单用户够不够** | 想给多人用 | user_interest 表已留 user_id 字段，加登录即可扩展 |
| **点赞数据太少学不准** | 前几天点赞 < 10 次 | 冷启动用默认权重，积累够了再生效 |
| **DeepSeek 打分太慢** | 每天几十条逐条打分耗时 | 批量打分（一次 prompt 塞多条），或只对候选 top-30 打分 |

---

## 成本估算

| 项目 | 成本 |
|---|---|
| Glance 自托管 | $0（Docker，几百 MB 内存） |
| DeepSeek 打分 | ~$5-10/月（每天几十条，复用现有 key） |
| GitHub Actions | $0（公开仓库免费额度够） |
| 服务器（跑 Glance + extension） | 看你部署在哪，Vercel/自己的 VPS 都行 |
| **合计** | **~$10/月**（主要是 LLM 调用） |

---

## 不做什么（避免过度设计）

- ❌ 复杂推荐算法（协同过滤/深度学习）—— 种子期点赞数据太少，简单加权够用
- ❌ 多用户系统 + 登录 —— P1 先你自己用，验证有效再扩
- ❌ 实时推送 —— 日报频率够了，别做 push 通知增加复杂度
- ❌ 自建前端框架 —— 这次就是为了用 Glance 省掉前端维护

---

## 下一步（P0 第一件事）

1. 本地 Docker 跑起来 Glance，玩 10 分钟熟悉它
2. 写最小 extension demo，验证能不能嵌入带按钮的 HTML
3. 验证通过 → 进 P1；不通过 → 走 Plan B（链接式点赞）

> 遵循"装新依赖先 30 分钟 spike"原则：先用最小 demo 验证 Glance extension 可行，再投入写完整功能。
