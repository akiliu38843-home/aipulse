# AIPulse 迭代建设计划

> 立项 2026-06-14 · 依据「迭代路线决策页」用户勾选的 11 项
> 产品定位：**中文世界的 AI 前沿信息站** —— 把英文圈最好的 20+ 信源，每天「翻译 + 去重 + 排重要性」端到中文读者面前，并独家关联「这对你的技能/职业意味着什么」。
> 节奏约定：**从 P0 起逐项做，每完成一档先给用户确认效果再继续**。

---

## 进度总览

| 档 | 项 | 状态 |
|---|---|---|
| P0 | 去重聚类 | ✅ 已完成待确认 |
| P0 | 今日头条置顶 | ✅ 已完成待确认 |
| P0 | 修导航一致性 | ✅ 已完成待确认 |
| P1 | 重要度排序 · 今日热度榜 | ✔️ 已确认 |
| P1 | AI 分类替代关键词 | ✔️ 已确认 |
| P2 | 定时自动抓取 | ✅ 已完成待确认 |
| P2 | 时间视图（今天/本周/本月） | ✅ 已完成待确认 |
| P2 | 信源健康徽章 | ✅ 已完成待确认 |
| P3 | 按主题订阅（非点赞） | ✅ 已完成待确认 |
| P3 | 已读 / 收藏（轻量） | ✅ 已完成待确认 |
| P4 | 加非英文源 + 社区源 | ✅ 已完成待确认 |
| P5 | 定制新闻：AI 工具定价面板 | ✅ 已完成待确认 |

状态图例：⬜ 待做 · 🔨 进行中 · ✅ 已完成待确认 · ✔️ 用户已确认

---

## P0 · 让常规新闻从「一堆」变「一份简报」

这一档是整套路线的地基，三项一起改造「常规新闻」页（`build_news.py` 里 `nl_bands` 那段 + 新闻页生成段）。

### P0-1 去重聚类
- **用户得到**：同一条新闻被好几家报道时，合并成一张卡，标「🔗 N 家在报道」，展开能看到各家标题+原文链接。页面从「噪音堆」变「精选」。
- **怎么做**：把 108 篇全局打散 → 用「标题共享实词 + 共享实体（OpenAI/Claude…）」做相似度判定 → 并查集（union-find）聚成簇 → 每簇选一个代表标题。**纯本地算法，不额外烧 API**，阈值可调、过程透明（符合"诚实/可解释"偏好）。
- **防误合**：阈值偏保守——要求共享实词 ≥3，或（共享实体 + 共享实词 ≥2）。宁可漏合、不可错合（把两条不同的 OpenAI 新闻并成一条体验更差）。
- **副产品**：簇大小 = 重要度信号，直接喂给 P1 的热度榜。
- **验收**：实测 18 对重复应被收进簇；随机抽查 10 个多源簇，无明显错合；页面卡片数明显少于 108。

### P0-2 今日头条置顶
- **用户得到**：页面顶部一块「今日头条」，列当天被报道最多的 2–3 条（最大的几个簇）。把「一堆」重新框成「一份简报」，同时直观证明去重在起作用（"6 家都在报道 → 它就是头条"）。
- **怎么做**：复用 P0-1 的聚类结果，按簇内信源数排序取 Top 3，渲染到新闻页 hero 区。**自洽**——头条就是这页内容自己最热的，不依赖另一条 daily_digest 管线。
- **验收**：hero 显示 Top 3 簇，每条标「N 家在报道」，点击跳代表原文。

### P0-3 修导航一致性
- **用户得到**：首页和新闻页导航统一成三个 tab：`直面就业 · 定制新闻 · 常规新闻`，消除「像 bug」的摩擦。
- **怎么做**：改 `build_site.py` 第 275–276 行的 `tab_slot`（首页那两个旧 tab → 三个），跑一次 `build_site.py zh/en`。`build_news.py` 那侧上一轮已经改好。
- **验收**：三个页面顶部 tab 完全一致，互相点得通，当前页高亮正确。

---

## P1 · 把「信息」变「判断」

### P1-1 重要度排序 · 今日热度榜
- **用户得到**：一个不按时间、按「重要度」排的榜。回答「我只有 5 分钟该看啥」。
- **怎么做**：重要度 = 簇内信源数（被几家报道）× 权重 + HN 赞数 + 新鲜度。做成一个置顶 band 或独立视图。
- **验收**：榜单顺序符合直觉（大新闻在前），可解释每条为何上榜。

### P1-2 AI 分类替代关键词
- **用户得到**：分类更准，「其他资讯」兜底箱（现塞 15 篇）大幅缩小。
- **怎么做**：翻译时顺带让 DeepSeek 归类（同一次 API 调用返回 译文+类别），替换现在的关键词硬匹配。带缓存，不重复烧钱。
- **验收**：「其他资讯」≤5 篇；抽查分类准确率明显高于关键词版。

---

## P2 · 让人养成每天来的习惯

### P2-1 定时自动抓取
- **怎么做**：launchd 定时任务，**仅工作日早晨**跑 `fetch_newsletters.py` + `build_news.py`（遵守"工作时间外不做后台敏感活动"+"配套定时开关"约定）。带一键开/关脚本。
- **验收**：plist 装好、能手动触发验证、有卸载方式。

### P2-2 时间视图（今天 / 本周 / 本月）
- **怎么做**：抓取结果按日期归档，前端加时间切换。错过一天能补。
- **验收**：三个时间档切换正常，历史可回溯。

### P2-3 信源健康徽章
- **怎么做**：每行/每页标「最近抓取于 X 小时前」+ 信源存活状态。
- **验收**：徽章显示真实抓取时间，挂掉的源有提示。

---

## P3 · 个性化 2.0（用对方式做回「点赞」）

### P3-1 按主题订阅（非点赞）
- **怎么做**：让用户勾「只看 研究/开源/商业…」，**生成时或前端过滤**，绕开当初点赞按钮死在 iframe CSP 的坑。
- **验收**：勾选后只显示选中主题，状态可存。

### P3-2 已读 / 收藏（轻量）
- **怎么做**：localStorage 存已读/收藏态（本地静态站，非 artifact，localStorage 可用）。
- **验收**：标记后刷新仍在，可清空。

---

## P4 · 扩规模 · 建护城河

### P4-1 加非英文源 + 社区源
- **怎么做**：机器之心（需写网页抓取，RSS 不可用）、Reddit r/LocalLLaMA、X 列表、Discord 摘要（学 Smol AI 模式）。信源越广越难被替代。
- **验收**：新增 ≥2 类非 RSS 源，纳入统一去重+翻译流。
- **已实现（2026-06-14）**：
  - ✅ **量子位 RSS** — 中国最大 AI 科技媒体（qbitai.com），纳入 RSS 信源列表，ok:8
  - ✅ **HuggingFace Daily Papers**（JSON REST API 类）— 每日 AI 论文精选，免认证，ok:12
  - ✅ **GitHub Trending AI**（web scraping 类）— 过滤 AI/ML 仓库，ok:8
  - ❌ Reddit — 代理 IP 被封（403 Blocked），搁置
  - ❌ 机器之心 — 主内容在 pro.jiqizhixin.com（付费），公开内容为零，搁置

---

## P5 · AI 工具定价面板（定制新闻页）

- **用户得到**：定制新闻页顶部多一条可折叠的"AI API 定价快照"栏。折叠状态显示 4 个头部模型报价摘要，展开后完整对比旗舰/实用/推理/省钱四档共 15 个模型（上下文、输入$/M、输出$/M）。
- **数据来源**：OpenRouter 公开 JSON API（`/api/v1/models`，免认证，337 个模型，实时价格）。
- **每日自动更新**：`fetch_newsletters.py` 末尾新增 `fetch_openrouter_prices()` → 存 `data/ai_prices.json`，随每日 launchd 拉取自动刷新。
- **实现时间**：2026-06-14。
- **覆盖模型**：Claude Fable 5 / GPT-5 Pro / Grok 4 / Claude Sonnet 4.6 / GPT-5 / GPT-5.5 / Gemini 3.5 Flash / DeepSeek R1 / o3 / o4-mini / Claude Haiku 4.5 / GPT-5 nano / GPT-4o mini / Llama 4 Maverick / Qwen3 Max。
- **展开状态用 localStorage 持久化**（`aipulse-pricing-open`），刷新后保留用户偏好。

---

## 关键技术决策记录

- **去重用本地算法不用 LLM**：108 篇两两比对成本可接受，纯本地、可解释、可调阈值，符合"诚实优于补丁"。LLM 聚类留作 P1 分类时的备选。
- **今日头条 = 最大的簇，不接 daily_digest**：让新闻页自洽，头条就是这页自己最热的，且复用去重成果、强化去重价值的可见性。
- **个性化避开 iframe CSP**：当初点赞按钮死因。改用生成时过滤 / 本地静态站 localStorage，从源头绕开。
